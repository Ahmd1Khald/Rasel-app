/*ThemeData lightTheme = ThemeData(
  scaffoldBackgroundColor: Colors.white,
  brightness: Brightness.light,
  primarySwatch: Colors.blue,
  primaryColor: AppColors.blue,
  appBarTheme: AppBarTheme(
    backgroundColor: AppColors.blue,
    iconTheme: const IconThemeData(color: Colors.white),
  ),
  bottomNavigationBarTheme: BottomNavigationBarThemeData(
      backgroundColor: AppColors.blue,
      selectedItemColor: Colors.white,
      unselectedItemColor: Colors.grey[400],
      showSelectedLabels: false,
      // hide labels for selected items
      showUnselectedLabels: false,
      // hide labels for unselected items
      selectedIconTheme: const IconThemeData(
        size: 26,
      )),
  bottomAppBarTheme: BottomAppBarTheme(color: AppColors.blue),
  drawerTheme: const DrawerThemeData(
    backgroundColor: Colors.white,
    //endShape:
  ),
  iconTheme: IconThemeData(
    color: AppColors.backgroundColor,
    size: 30.sp,
  ),
  textTheme: TextTheme(
      //name in drawer
      bodyLarge: AppStyles.postUserName,
      //lists in drawer
      labelLarge: AppStyles.drawerStyles,
      //user name in posts
      //displaySmall: AppStyles.postUserName,
      //post hint
      bodySmall: TextStyle(
        color: HexColor('#E9EDF1'),
      ),
      //post contents
      bodyMedium: AppStyles.postDate),
);

ThemeData darkTheme = ThemeData(
  scaffoldBackgroundColor: AppColors.backgroundColor,
  brightness: Brightness.dark,
  appBarTheme: AppBarTheme(
    backgroundColor: AppColors.backgroundColor,
    iconTheme: const IconThemeData(color: Colors.white),
  ),
  bottomAppBarTheme: BottomAppBarTheme(color: AppColors.backgroundColor),
  textTheme: TextTheme(
      //name in drawer
      bodyLarge: AppStyles.postUserName,
      //lists in drawer
      labelLarge: AppStyles.drawerStyles,
      //user name in posts
      //displaySmall: AppStyles.postUserName,
      //post hint
      bodySmall: TextStyle(
        color: HexColor('#E9EDF1'),
      ),
      //post contents
      bodyMedium: AppStyles.postDate),
);*/
